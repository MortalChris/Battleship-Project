import {placeShipsonBoard} from '../index.js';

test("This should place a ship on array at row3-col4 to row3-col7",() =>{
    expect(placeShipsonBoard(gameBoardArray, 2, 3, 'horizontal', 4)).toBe();
})
